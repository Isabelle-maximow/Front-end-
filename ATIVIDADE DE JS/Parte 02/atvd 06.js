// 6. Crie um programa que receba um número e diga se ele é positivo, negativo ou
// zero.
let numero = -5;
let resultado = numero > 0 ? "positivo" : numero < 0 ? "negativo" : "zero";

console.log(`o numero é ${resultado}`);